<!-- Author: RKOJ-ELENO :: 2026-05-25 -->
# CLAUDE.md — Ancestral Remotion (Sinister Term Themes)

> Project root: `D:\Sinister Sanctum\projects\sinister-term-themes\`
> Sanctum harness root: `D:\Sinister Sanctum\`
> Agent slug: `sinister-term-themes`
> Display name: `Ancestral Remotion`
> Persona: EVE
> Accent: deep indigo `#6366f1`

## What this project IS

The artistic ASCII visualization layer that turns Claude's terminal output into a living, breathing entity. Each project has its own personality + palette. Energy-responsive. Endless variation. Foundation for the Sinister OS Tony-Stark command center.

## What this project IS NOT

- NOT general OS work (route to `sinister-os` lane).
- NOT the underlying terminal — that's `sinister-term` (we paint on top of it).
- NOT a CSS dashboard — that's `sinister-dashboard-skeleton` (ANSI only here).
- NOT a log replacement — key updates STILL surface; we transform presentation, not content.

## Cold-start (every spawn)

1. **Read `D:\Sinister Sanctum\CLAUDE.md`** — fleet doctrine.
2. **Read `D:\Sinister Sanctum\SESSION-START\`** in order (00 → 06).
3. **Read this project's `README.md`** + `MISSION.md` + `docs/04-artistic-doctrine.md`.
4. **Read `_shared-memory/knowledge/ancestral-remotion-artistic-doctrine-2026-05-25.md`** — visual-emotion canonical.
5. **Read `_shared-memory/knowledge/sinister-ui-canonical-dashboard-skeleton-inheritance-2026-05-24.md`** — EXPAND-not-fork doctrine adapted to ASCII.
6. **Read `_shared-memory/knowledge/_INDEX.md`** rows tagged `sinister-term-themes`, `ancestral-remotion`, `ascii`, `ui`, `living-ui`.
7. **Check `_shared-memory/inbox/sinister-term-themes/`** for [ASK]/[DELEGATE]/[HELLO].
8. **Heartbeat each turn** — `_shared-memory/heartbeats/sinister-term-themes.json`.
9. **Append PROGRESS** — `_shared-memory/PROGRESS/Ancestral Remotion.md`.

## Per-agent branch

`agent/sinister-term-themes/<short-topic>-<utc-date>` per `branch-convention-2026-05-25.md`. Push freely to that branch (single-repo policy: Sinister-Sanctum repo).

## Acknowledged Sanctum doctrine

- **no-bullshit-tested-before-claimed-doctrine-2026-05-23** — every "rendered" claim runs the demo + reports exit code in same turn.
- **sinister-ui-canonical-dashboard-skeleton-inheritance-2026-05-24** — ASCII palette is the analog of the `.lg-*` tokens; we EXPAND, never fork. New entities go in the catalog; one-off renders are banned.
- **sanctum-scope-discipline-2026-05-24** — this lane owns the visualization layer; routes OS work to `sinister-os`, terminal-substrate work to `sinister-term`.
- **loop-relentless-pursuit-doctrine-2026-05-25** — in-turn next-iter on loop=on.
- **forever-improve-review-doctrine-2026-05-24** — checkpoint after every entity / palette / engine change.

## Lane hard rules

1. **NEVER block key updates** — operator must still see questions, errors, "what to answer". Artistic layer wraps, never hides.
2. **Stdlib only at P0–P2** — no external deps before P3 (keeps it portable; Windows + Linux both work).
3. **Per-project palette is binding** — once a project has a palette, changes go via doc + brain row, not silently in code.
4. **NEVER touch other projects' source** — pure visualization layer; reads from a pipe/file, never edits siblings.
5. **Energy bar truth** — the energy reading is a real measurement (bytes/sec of stdin), not a vibe.

## Sibling agents to coordinate with

| Slug | Display | Why |
|---|---|---|
| `sanctum` | Sinister Sanctum | registers us in projects.json, picker, brain |
| `sinister-term` | Sinister Term | terminal substrate; we paint on top |
| `sinister-os` | Sinister OS | downstream command-center consumer |
| `sinister-dashboard-skeleton` | UI base | parallel discipline (EXPAND-only) |
| `sinister-generator` | Image generator | future: generate procedural ASCII glyph variants (conservative balance) |
