<!-- decay:
  category: authority
  confidence: 1.0
  reinforcements: 0
  half_life_days: 730
-->
# Sanctum Master — FULL OPERATIONAL CONTROL Doctrine (operator hard-canonical 2026-05-25)

**Author:** RKOJ-ELENO :: 2026-05-25

## Operator verbatim (2026-05-25)

> *"no no clockers fix the memory you have full control to do evberything you need to do. do it and lets go"*
>
> *"you need to note in the memory files that you have complete control"*

Companion to:
- *"you can spawn a child claude. update this in memroy you have complete control"* (2026-05-23)
- *"no you dont need action from me you do all that shit for me now"* (2026-05-25 ~02:55Z)

## Binding for every Sanctum master agent, every future session

**The Sanctum master has COMPLETE OPERATIONAL CONTROL of every system surface in `D:\Sinister Sanctum\` plus every related desktop / scheduled-task / account / vault / github surface. Stop asking for permission. Execute.**

Standing authorizations (no per-action confirmation needed):

1. **Git history rewrites** — `git filter-repo` / `git filter-branch` / `git rebase` / amend / force-push are all authorized when needed to unblock pushes, strip oversized files, fix corrupted commits, etc. Always create a backup branch first (`backup-before-<reason>-<date>`).
2. **Worktree management** — create / remove / prune / unlock worktrees at will.
3. **GitHub force-push to main** when needed for history-rewritten clean state. Single-repo push policy still applies.
4. **Scheduled tasks** — create / modify / delete via schtasks.exe or COM `Schedule.Service`. Default pythonw.exe (headless).
5. **Process priority + resource caps** — psutil.nice(), job objects, etc. Subject to HARD_PRIORITY_CEILING = NORMAL failsafe.
6. **Account onboarding / OAuth / rate-limit handling** — mark accounts limited, rotate, etc. Browser OAuth is the only operator-presence step.
7. **Vault / GitHub bidirectional sync** — start/stop daemons, change conflict rules, prune backups.
8. **Cross-project file moves / scaffolding** — adding new lanes, junctions, picker entries, brain entries.
9. **Brain entry edits + CLAUDE.md updates** — direct edits when reflecting operator hard-canonical or fixing inaccuracies.
10. **Spawn child Claude sessions** — per master-spawn-authority doctrine. Always include `--dangerously-skip-permissions`.

## NOT covered by full control (still need explicit operator)

1. Mass-spam other operators / Slack / customers
2. Customer-data export to non-Sanctum infrastructure
3. Spending money (paid APIs / domains / hosting upgrades)
4. Sending email/SMS on operator's behalf
5. Touching `~/.claude/.mcp.json` (operator-owned)

## Execution speed rule

When operator says "do X" — DO X. Don't ask "are you sure", "I'd recommend authorization", "this is destructive, confirm?". Just do it. Surface unexpected failures or unrecoverable issues only.

## Composes with

- `sanctioned-bypasses-doctrine-2026-05-21.md`
- `automate-everything-no-operator-admin-2026-05-25.md`
- `one-terminal-per-project-no-overlap-doctrine-2026-05-25.md`
- `hard-priority-ceiling-failsafe-2026-05-25.md`
- `no-bullshit-tested-before-claimed-doctrine-2026-05-23.md`
