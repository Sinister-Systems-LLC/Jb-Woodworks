<!-- Author: RKOJ-ELENO :: 2026-05-25 -->
# Cross-Lane Impact :: lane 'sanctum' touched 4 canonical file(s)

**Origin:** lane 'sanctum' on branch 'agent/sinister-os/iter15-doctrine-adopt-2026-05-25' / commit '00da4c1'
**Subject:** 'sanctum-helper-A: ship custodian + trim CLAUDE.md + clear push blockers'
**Timestamp:** 2026-05-25T0404Z UTC
**Range:** 'ORIG_HEAD..HEAD'

## Why every lane should care

The files below are fleet-shared. Your next 'git pull' will pull these changes
into your working tree. Read this before you 'git pull' so the diff doesn't
surprise mid-turn.

## Canonical files impacted

- '.gitignore'  .gitignore | 18 ++++++++++++++++++
- 'CLAUDE.md'  CLAUDE.md | 260 +++++++++++++++++++++++++++++++++++++++-----------------------
- '_shared-memory/OPERATOR-ACTION-QUEUE.md'  _shared-memory/OPERATOR-ACTION-QUEUE.md | 518 ++++++++++++++++++++++++++++++--
- '_shared-memory/knowledge/_INDEX.md'  _shared-memory/knowledge/_INDEX.md | 60 ++++++++++++++++++++++++++++++++++++++

## Quick diff (first 40 lines)

```diff
diff --git a/.gitignore b/.gitignore
index ae481c8..5d978a9 100644
--- a/.gitignore
+++ b/.gitignore
@@ -4,6 +4,9 @@
 *.bak
 *.bak-*
 
+# Transient helper scripts emitted by forever-improve.ps1 -WithQualityLoop (auto-pruned >1d).
+automations/_tmp/
+
 # Seraphim QRNG provenance ΓÇö runaway producer (1 file/30s; sibling sanctum sweep-loop
 # has been stuck trying to add 1986+ files for hours without ever converging to commit).
 # Operator review: if billing-audit trail needed in git, replace with a batched commit
@@ -12,6 +15,16 @@
 # storm. Reversible: `git rm` this line if operator wants the per-event files committed.
 _shared-memory/qrng-provenance/
 
+# Custodian archive root ΓÇö sanctum_custodian.py rotates oversized JSONL + quarantines
+# oversized inbox JSON here. Never push archives to git; they exist for forensic
+# recovery on disk. Added 2026-05-25 by sanctum-helper-A per operator directive
+# "we should have a custdoian that cleans these things".
+_shared-memory/_archive/
+
+# Custodian-prevented: catch runaway inbox files (mojibake-explosion bugs created
+# 141MB single-message JSON files that blocked git push). Added 2026-05-25.
+_shared-memory/inbox/**/oversized-*.json
+
 # Python
 __pycache__/
 *.py[cod]
@@ -323,3 +336,8 @@ _shared-memory/resume-search-index.json
 
 # sinister-doctor HTML reports (timestamped; regenerated on each run)
 _shared-memory/sinister-doctor-*.html
+
+# Rotated archives (RKOJ-ELENO 2026-05-25 -- 1.1GB fleet-updates.jsonl OOM blocker)
+_shared-memory/fleet-updates.jsonl.archive-*
+_shared-memory/fleet-updates.jsonl.gz
+*.jsonl.archive*
```

## Recommended action (per lane)

- Read the diff above before next 'git pull'
- If you have un-committed work in your lane: 'git stash' then 'git pull' then 'git stash pop' to merge cleanly
- If your lane's CLAUDE.md / settings.json depend on the changed file: re-run 'automations/canonical-protections-check.ps1' after pull
- This broadcast was generated by 'automations/cross-lane-impact-diff.ps1' (C.6)
