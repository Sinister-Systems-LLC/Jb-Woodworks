---
format_version: 2
author: RKOJ-ELENO
slug: sinister-term
heading_id: 2026-05-26-iter-0076-b838a0
saved_at: 2026-05-26T21:11:31Z
length: 1955
category: fact
confidence: 0.500
trust: medium
source: adoption-sweep
---

# sinister-term :: 2026-05-26 21:00 — shipped: iter-76 /progress-of <slug> peer-PROGRESS drilldown (alias /po)

Operator-facing peer-PROGRESS reader. `/progress-of` (alias `/po`) reads any agent's PROGRESS log under `_shared-memory/PROGRESS/<Display>.md` so the operator (and any agent doing a sister-work review) can catch up on what others shipped without leaving sterm. Companion to existing `/progress` (our own log). Three forms: `/po` (newest-first listing of all agent PROGRESS logs as `slug<28>  age ago  size KiB`), `/po <slug>` (top 5 `## ` entries newest-first), `/po <slug> N` (limit 1..50, clamped). Slug derivation: filename stem lowercased + spaces/underscores → hyphens (e.g. `Sinister OS.md` → `sinister-os`, `Eve EXE.md` → `eve-exe`). Exclusions: `_TEMPLATE.md`, `README.md`, and anything not `*.md` (skips the `.log` artifact files). Lookup: exact-slug first, substring fallback with ambiguous-multi candidates list + no-match available-list preview (12-slug cap). Leading-slash stripped (`/po /sinister-os` works). Case-insensitive. Tolerant: missing PROGRESS dir, empty log file, log with text but no `## ` headings, OSError on read, out-of-range limit (clamped to 1..50). Heading-split algo mirrors `cmd_progress` (preserves block bodies including blank-line separators). 19 unit tests in `tests/test_progress_of_builtin.py`: 3-agent listing with KiB + age columns, _TEMPLATE/README/.log exclusion, mtime-newest-first ordering, exact-slug default-5, explicit limit, substring unique, substring ambiguous returns candidates, no-match available-list, invalid int rejected, limit clamping (999 → 50), missing-dir msg, empty-log msg, no-`## `-headings msg, case-insensitive (`SINISTER-OS`), leading-slash strip, /-prefix dispatch, /po alias dispatch routing, /help inclusion, slug kebab conversion (Eve EXE → eve-exe), template+readme never resolvable as a slug. pytest -q: 623 passed (was 604, +19). Composes with existing `/progress` (our own) — the triad: `/progress` (mine) + `/progress-of <slug>` (one other) + `/agents` (multi-agent metadata).
