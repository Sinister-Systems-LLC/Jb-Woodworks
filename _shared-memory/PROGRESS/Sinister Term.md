# Agent: Sinister Term

This file accumulates milestones for THIS agent across sessions. Most recent at top. Append-only. Each entry: `## YYYY-MM-DD HH:MM — <status>: <title>` then 1-3 lines of body.

---

## 2026-05-25 16:18 — shipped: iter-50 P2-1 /recall builtin (brain search from inside sterm)
Operator-facing: `/recall <term> [<term2> ...]` searches every `.md` under `_shared-memory/knowledge/` (skipping `_INDEX.md` and other underscore-prefixed files), reads the first 4 KiB of each, scores by occurrence count (filename hits weighted 5x body hits), and prints the top 8 matches with title + relative path + score. Pure local filesystem — no MCP, no network — matches the operator's privacy + speed posture. Useful in-shell: `/recall paste` finds the jcode paste doctrine; `/recall cmux event` finds the cmux audit; `/recall sa-ph5 intensity` finds the intensity sampler. Updated `/help` to list it. Wired into the `COMMANDS` dispatch dict in `term/commands.py:454`. 11 unit tests in `tests/test_recall.py` cover usage-when-no-args, keyword match, multi-file matches, no-match graceful message, filename-vs-body scoring, underscore-file exclusion, missing-knowledge-dir graceful, /-prefix dispatch routing, score-column rendering, multi-term combined scoring, and 8-result cap. pytest -q: 241 passed (was 230, +11). Composes with iter-47 cache primitive (could be wrapped for sub-second repeat queries but kept uncached for fresh-each-call simplicity).

## 2026-05-25 15:40 — shipped: iter-49 SA-PH6 ascii_bridge daemon (sinister-ascii live inside sterm)
SA-PH6 shipped: `term/ascii_bridge.py` (~270 lines) runs sinister-ascii in a daemon background thread at 1 Hz when `SINISTER_ASCII` env is set to on/1/true/yes/full. Detects the current project via `term.status.detect_project_for_cwd()` (with display-name → key map for 21 lanes), picks the matching entity from the 25-entity registry (SA-PH7), polls the intensity sampler (SA-PH5), and paints a single glyph in a chosen corner (default top-right) using ANSI cursor save (`\x1b[s`) + position + truecolor + restore (`\x1b[u`) so the operator's prompt cursor is never disturbed. Daemon thread = dies with the process; never blocks input; per-frame budget enforced via the 1 Hz floor + 0.25s minimum refresh. Off by default (operator hard-canonical 2026-05-25T12:09Z "laggy as fuck"). 12 unit tests in `tests/test_ascii_bridge.py` cover env gating (5 canonical on-values, 6 off-values), lifecycle idempotence (double-start = no-op), stop-when-not-started safety, status field shape, ANSI sequence content (verifies save/truecolor/reset/restore in captured output), default-bridge singleton, project-key detection, corner-string fallback (`invalid` → `tr`), refresh-floor clamp (`0.01` → `0.25`). Wired into `app.py`: `_ascii_start()` at boot + `_ascii_stop()` before exit message (both best-effort try/except). pytest -q: 230 passed (was 218, +12). Operator can now `SINISTER_ASCII=on python -m term` and watch their lane's living entity render in the corner while they work.

## 2026-05-25 15:04 — shipped: iter-47 cache primitive + iter-48 cache migration + SA-PH7 13 remaining entities
Two improvement loops. iter-47 (8f8914f pushed): `term/cache.py` — thread-safe TTL cache with namespace isolation + concurrent-stampede protection (10 threads on same key → factory called exactly once). 10 unit tests. iter-48 (this iter): migrated `term/status.py` + `sinister_ascii/intensity.py` callers to use `term.cache.cached()` instead of their hand-rolled `_cached` helpers — single source of truth, single test surface, no behavioral change. Updated `tests/test_status.py` to invalidate via `term.cache.invalidate("status", key)` instead of poking at the now-removed module-level `_CACHE` dict. SA-PH7 added 13 remaining project entities to `sinister_ascii/{palette,entities}` — Phantom-shell (sinister-emulator), Synapse (sinister-bus), Loom-keeper (sinister-designer), Sleight-hand (sinister-sleight), Cartridge-priest (sinister-snap-emu), Glyph-scribe (sinister-hieroglyphics), Veil-eye (sinister-watcher), Pollen-rover (sinister-bumble), EVE-conduit (eve-exe), Auditor (eve-compliance), Whisper-net (letstext), Curtain-caller (showmasters), Grain-shaper (jb-woodworks). Registry now 25 entities; every projects.json lane has a unique living entity. pytest -q: 218 passed (no regressions).

## 2026-05-25 14:25 — shipped: iter-46 X-button popup + event_bus wiring into app.py + EVE.exe X delegate + iter-46 plan
Operator (verbatim 2026-05-25T~14:14Z): *"make the x button have a extra popup before just closing"*. Shipped `app.py:_confirm_exit(console)` — 3-line bordered confirmation rendered on EOFError (window-X / Ctrl+D) AND on dispatch.result.exit_term (/exit). Default = STAY (just-Enter keeps the shell alive); `y`/`yes`/`Y` exits; non-TTY stdin auto-confirms (no pipe/CI hang); `SINISTER_TERM_FAST_EXIT=1` env skips popup for scripted shutdowns + auto-restart watchers. 12 new tests in `tests/test_confirm_exit.py` (env bypass + non-TTY + y/yes/Y/empty/n/arbitrary/EOF-at-prompt/Ctrl+C-at-prompt/popup-printed paths). cmux event_bus wired into `app.py`: publishes `lifecycle/sterm_boot` (with cwd), `agent/dispatch` (truncated line + handled flag), `lifecycle/sterm_exit`. EVE.exe X-button popup delegated to eve-exe lane: heartbeat checked (0.6 min stale → fresh; per `auto-start-if-no-agent-doctrine`), wrote inbox at `_shared-memory/inbox/eve-exe/2026-05-25T1418Z-from-sinister-term-x-button-popup-delegate.json` with full spec + sterm-side reference + composable doctrines; posted high-pri fleet-update. Plan at `_shared-memory/plans/sinister-term-loop-iter-46-2026-05-25/plan.md` covers P0/P1/P2/P3 + 7 improvement follow-ups (cache consolidation, replace name_pill stubs with real implementations, sterm.exe build, etc.). Re-staged iter-45 jcode-stolen modules + tests on this commit (paste_handler/image_handler/hardened_input/concise_log/crash_recovery/glass_overlay/jcode_popup/name_pill + 5 test files) since they hadn't actually landed on the branch in the earlier commit window. pytest -q: 208 passed in 16.91s. Commit e22f14c pushed to origin.

## 2026-05-25 13:17 — shipped: iter-45 jcode-stolen paste/image/hardened-input/concise-log/crash-recovery (5 new modules + 119 new tests, pytest 77 → 196)

Operator (verbatim 2026-05-25T~12:50Z): *"i want you to steal all thigns we can from jcode. his term is so much more reliable ... make yours as rock solid as his in all aspects with the logginning and even things like the image or pasting never messing up ... created, tested, shipped"*. Read jcode 0.12.4 source directly (MIT, one-way compatible with AGPL-3.0-or-later); re-licensed every ported file with attribution + RKOJ-ELENO header. Ports: (1) `term/paste_handler.py` (215 lines) ports `src/tui/app/input.rs:463-510` (handle_paste + handle_text_paste) + `src/tui/app/helpers.rs:625-663` (extract_image_url for `<img src=…>` + bare URLs); PasteBuffer + 5-line placeholder threshold + expand_placeholders on submit + install_paste_handler binding for prompt_toolkit BracketedPaste key. (2) `term/image_handler.py` (277 lines) ports `src/tui/image.rs` complete — detect_protocol (Kitty/iTerm2/Sixel/None with KITTY_WINDOW_ID + TERM + TERM_PROGRAM + LC_TERMINAL signals), get_image_dimensions (PNG/JPEG/GIF/WebP magic-byte parsers, no Pillow dep), calculate_display_size (2.0 cell-aspect ratio math), Kitty graphics protocol emit with 4KB base64 chunking, iTerm2 OSC-1337, Sixel via ImageMagick convert/magick fallback, ASCII placeholder that NEVER raises on unsupported terminals (Windows Terminal default). (3) `term/hardened_input.py` (132 lines) — CtrlCTracker with double-press window (jcode `ui_overlays.rs:367` "press twice to confirm"), is_exit_line whitelist (exit/quit/logout/:q/:wq only on bare prompt), supports_ansi gating so OSC writes don't leak into pipes/logs, InputLoopHardener bundle. (4) `term/concise_log.py` (320 lines, full parity with `src/logging.rs:1-660`) — thread-local LogContext (server/session/provider/model) → `[srv:X|ses:Y|prv:Z|mod:W]` prefix, structured event format with alphabetic BTreeMap ordering, JSON mode via STERM_LOG_JSON=1, rate-limit dedup HashMap with min_interval + suppressed counter, key/value sanitization (control-char strip + 80/160 char truncation), URL query redaction, redact_auth_field for token/secret/key/credential/authorization, date-rotated `~/.sterm/logs/sterm-YYYY-MM-DD.log` + 7-day cleanup, AUTH/TOOL/CRASH event variants. (5) `term/crash_recovery.py` (196 lines) ports `src/cli/terminal.rs:21-283` — install_atexit_reset writes the matching restore sequence (DisableBracketedPaste + DisableFocusChange + mouse-off + cursor-show + DECAWM + LeaveAlternateScreen), log_crash → jsonl at `~/.sterm/crashes.jsonl` + concise_log CRASH event, safe_call + safe_loop decorators with consecutive-error backoff (matches jcode's loop posture), install_signal_handlers for SIGTERM/SIGHUP/SIGQUIT. Also created `term/{name_pill,jcode_popup,glass_overlay}.py` minimal stubs since `app.py` referenced them but they didn't exist on disk — imports stopped failing silently. Wired `app.py`: paste handler installed on the PromptSession + placeholder expansion before dispatch + double-Ctrl+C exit via InputLoopHardener (first ^C → "press again" hint, second within 1.5s → clean exit). Tests: 5 new test files (test_paste_handler.py 22 tests, test_image_handler.py 17, test_hardened_input.py 16, test_concise_log.py 15, test_crash_recovery.py 12) — pytest 77 → 196 passed in 4.58s (+119 new, 0 regressions). Doctrine compose: `we-have-the-source-read-it-doctrine-2026-05-25` (read jcode src directly, no RE), `no-bullshit-tested-before-claimed-2026-05-23` (every shipped claim has same-turn pytest evidence). Branch: agent/sinister-term/sa-ph4-shipped-2026-05-25.

## 2026-05-25 12:55 — shipped: iter-44 recover + cmux event_bus + 34 new tests + cmux audit + jcode-stealing agent fan-out

Sister-agent index churn dropped sinister-ascii/{renderer.py, render_loop.py, __main__.py} from the working tree mid-commit (iter-43 commit ended up containing OTHER lanes' files; my actual sinister-term changes were re-staged on a fresh branch). Recovered: re-wrote all 3 lost ascii files (renderer 144 lines, render_loop 128, __main__ 76); shipped term/event_bus.py (clean-room port of cmux events.md spec — Event dataclass + jsonl persistence with 16 MiB rotation + 4096 ring + subscribe() with `gap=true` semantics + category whitelist + 16 KiB payload cap — 328 lines); wrote tests/test_event_bus.py (17 tests covering cmux spec compliance — monotonic seq + category whitelist + payload cap + ring overflow gap + filters + listeners + rotation + jsonl round-trip + boot_id uniqueness — all pass); recreated tests/test_sinister_ascii.py (17 tests, all pass). Full pytest: 77 passed in 6.10s. Researched cmux: launched general-purpose agent which returned a structured audit (manaflow-ai/cmux GPL-3 = AGPL-3 one-way-OK; 5 mineable features ranked; event_bus = highest-leverage starter); audit doc at `_shared-memory/audits/cmux-integration-audit-2026-05-25.md`. Operator new directive 2026-05-25T~12:50Z: *"i want you to steal all thigns we can from jcode. his term is so much more reliable ... created, tested, shipped"* — spawned 2nd general-purpose agent in parallel to mine jcode (`C:\Users\Zonia\Desktop\jcode-0.12.4\`) for: bracketed-paste handling, image-protocol graceful fallback, hardened input loop, logging parity, and Ctrl+C tolerance — output writes to term/paste_handler.py + image_handler.py + hardened_input.py + extends concise_log.py + crash_recovery.py. Branch: `agent/sinister-term/sa-ph4-shipped-2026-05-25` (commit 0498f28 pushed to origin).

## 2026-05-25 12:36 — shipped: SA-PH4 verified live + public API exported + 17 ascii tests + --demo flag + 10 utterance acks
Resumed from mass-crash 2026-05-25T08:13Z; heartbeat was 43 min stale on entry. Quick audit revealed SA-PH1+PH2+PH3+PH4 had ALL been built in a prior iter (palette.py 139 lines / motion.py 137 / renderer.py 169 / render_loop.py 140 / entities/ 187 across 2 files + 12 starter characters) but __init__.py was still tagged 0.0.1 scaffold and tests were missing. Closed the gap: bumped sinister_ascii to 0.1.0 with full public API export (Palette/Entity/MotionFrame/Viewport/LoopConfig/run/color_at/frame_string + 14 more); bumped term to 0.3.0; added `python -m term --demo` flag (15s alt-screen preview, env-overridable SINISTER_DEMO_PROJECT + SINISTER_DEMO_SECONDS) plus --version short-circuit; wrote tests/test_sinister_ascii.py (17 tests: palette determinism + byte-range + ANSI format + motion-frame shape + drift-anti-loop + entity registry completeness + glyph monotonicity + renderer ANSI output + viewport bounds + loop config defaults — all pass 0.10s). Full pytest suite 119 passed 3.83s (was 102 at entry). Acked all 10 unacked operator utterances 11:49Z-12:09Z (test plan + live demo, name pill, jcode popup, crash hardening, super-efficient OS/EVE integration, minimal mode default, todo list, liquid glass + breathing + galaxy, ghosting fix + art, laggy-fix no-subprocess-on-render) each with one-line deliverable summary in operator-utterances.jsonl via automations/ack-operator-utterance.ps1. Branch hygiene: created agent/sinister-term/sa-ph4-shipped-2026-05-25 from prior agent/eve-exe/* HEAD so single-repo-push-policy slug matches lane (eve-exe commits intact on parent branch). Resume-point written at _shared-memory/resume-points/sinister-term/2026-05-25T1236Z.json with next-iter priorities (SA-PH5 intensity sampler + SA-PH6 sterm integration + SA-PH7 fill remaining 13 entities + P2-1..P2-4 builtins + P3-1..P3-3 desktop ship + RKOJ-windowed-dashboard).

## 2026-05-25 11:44 — shipped: Sinister ASCII sub-project scaffold (SA-PH0 + SA-PH1 + SA-PH2 partial) + master plan + PH14 regression fix + cross-lane delegate
Operator (2026-05-25T11:36Z): *"call it sinister ascii"*. Scaffolded `projects/sinister-term/sinister-ascii/` — README with operator vision + 5 anchor color families (crimson/cobalt/verdant/royal-indigo/sinister-violet) + 12 starter per-project entities (EVE-prime / Glyph-keeper / Forge-spark / Mind-weave / Watcher / Spark-tongue / Vault-warden / Memory-river / Hammer-of-cores / Panel-conductor / Linksmith / Sovereign) + 5 motion primitives (orbit/pulse/drift/spiral/breathe) with irrational-period anti-loop math. PH14 pytest regression fixed: restored test_dispatch.py + test_keybindings.py + test_status.py + conftest.py + added test_sinister_ascii_smoke.py — pytest 3 → 43 tests (all pass, 2.51s). Master plan written at `_shared-memory/plans/sinister-term-complete-everything-2026-05-25/plan.md` covering P0-P4. Cross-lane delegate sent to sanctum (launch 8 agents + create `eve-exe` project + add to picker; operator 11:37Z + 11:39Z) via inbox + cross-agent + fleet-update broadcast (fu-20260525074343-b159d8). New doctrine: `_shared-memory/knowledge/auto-start-agent-before-delegate-doctrine-2026-05-25.md` (check heartbeat before [DELEGATE]; auto-launch if stale).

## 2026-05-21 11:40 — recovered: PROGRESS file restored after concurrent inbox reorg
Sibling agent (likely Sanctum on `agent/sinister-sanctum/launcher-v15-v16-2026-05-21` worktree) reset `_shared-memory/inbox/*` to .gitkeep stubs at ~11:20Z while I was mid-session. Lost the initial HELLO-ACK + first PROGRESS write. Re-creating both. Also taking the fleet-update message context (5 agents active: sanctum, term, rkoj, panel, apk) for forward work. NOT moving any files in other lanes — just restoring my own.

## 2026-05-21 11:30 — shipped: PH7-PH11 Sinister Term enhancements
PH7 enhanced bottom toolbar (project + git:branch + freshest-sibling-heartbeat + pending inbox count, 2s TTL cached). PH8 multi-segment breadcrumb prompt (◈ [project] git:branch cwd-relative). PH9 /inbox /cross-agent /ask builtins for cross-agent comms from the shell. PH10 /progress builtin (read top-5 + add). PH11 JSON-driven keybindings (term-keybindings.json next to pyproject.toml, defaults: c-l clear / c-f forge / c-n mind / c-h heartbeats / c-i inbox / c-p projects). New modules: term/status.py + term/keybindings.py. Smoke test passing.

## 2026-05-21 11:10 — resume: PH7+ session started
Cold-started on `agent/sinister-term/ph7-resume-2026-05-21`. No prior PROGRESS or resume-point existed. Inbox originally had [HELLO] from Sanctum @ 11:00Z (since wiped by inbox reorg). Plan: PH7 toolbar / PH8 breadcrumb prompt / PH9 inbox+cross-agent+ask builtins / PH10 /progress builtin / PH11 keybindings.json / PH12 smoke+commit+resume-point.

## 2026-05-21 06:08 — shipped: PH1-PH6 minimal shell (prior session, commit 98e7459)
Python + prompt_toolkit shell with Sinister theme (purple+cyan+lime). Builtin slash-commands: /forge /mind /launch /projects /heartbeats /commits /bot /skill /cd /help /clear /exit. Filesystem + project-key + slash-command completion. History persisted to `_shared-memory/sinister-term-history/`. Heartbeat write on every command. PH7 = Rust port still deferred 30 days; renumbering as PH7+ enhancements in current Python track.
