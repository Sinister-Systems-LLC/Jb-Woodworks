# Scribe - absorbed facts (operator-reviewable; one fact per line)

(This file is populated by `scribe.absorb(fact, source, tags=[])` MCP calls.
Each line is `- [<ISO>] <fact> (source: <source>) [tags: <csv>]`.
Hand-edit if you want; the loader picks up changes on next call.
Delete with `scribe.forget(<substring>)` - the deletion is audit-logged too.)

