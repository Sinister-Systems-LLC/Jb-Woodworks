# Sentinel - operator-curated alarm metadata

(Tier-1 bots don't load this into an LLM; this file documents recurring patterns
the operator wants Sentinel to remember + apply when adding new alarms.)

